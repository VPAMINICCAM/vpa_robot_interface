# VPA Robot Car Interface

The package of VPA Car interface aims to contains all sensors, actuators drivers in one package. Thhis package is therefore free of any high-level logic but only performing commands from other sensors.

This package is again branched by the type of robot.

## Jetracer

The majority of research and more advanced studies in this lab is planned on the jetracer with Latrax Rally chassis with additional sensors. Given the limitation of the offical support in OS of the Jetson Nano developer kit. The interface is developed in container to resolve the version conflicts.